<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Game Opening - Bosque</title>

  <!-- Bootstrap CSS via CDN -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />

  <!-- anime.js CDN -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/animejs/3.2.1/anime.min.js"></script>

  <style>
    /* Fundo bosque com imagem */
    body, html {
      height: 100%;
      margin: 0;
      background: url('https://images.unsplash.com/photo-1506744038136-46273834b3fb?auto=format&fit=crop&w=1470&q=80') no-repeat center center fixed;
      background-size: cover;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      display: flex;
      align-items: center;
      justify-content: center;
      overflow: hidden;
      color: #e6f2e6; /* Verde clarinho */
      text-align: center;
      user-select: none;
      /* efeito leve escurecido no fundo */
    }

    /* Camada escura para contraste */
    body::before {
      content: "";
      position: fixed;
      top: 0; left: 0; right: 0; bottom: 0;
      background-color: rgba(10, 40, 10, 0.55);
      z-index: 0;
    }

    .container-opening {
      position: relative;
      z-index: 1;
      max-width: 400px;
      background: rgba(25, 50, 20, 0.65);
      border-radius: 20px;
      padding: 40px 30px;
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.6);
      backdrop-filter: blur(8px);
      color: #d4e7d4;
    }

    /* Logo estilizado com cor natural */
    .logo {
      font-size: 3.5rem;
      font-weight: 900;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      margin-bottom: 15px;
      color: #bde0a8; /* verde suave */
      cursor: default;
      text-shadow: 2px 2px 6px rgba(0,0,0,0.7);
    }

    .subtitle {
      font-size: 1.2rem;
      font-weight: 500;
      margin-bottom: 35px;
      color: #c7d9b5;
      text-shadow: 1px 1px 3px rgba(0,0,0,0.5);
    }

    /* Botão estilizado com cores bosque */
    .btn-play {
      font-size: 1.3rem;
      font-weight: 700;
      padding: 12px 40px;
      border-radius: 50px;
      background: linear-gradient(45deg, #6a994e, #a7c957);
      border: none;
      color: white;
      box-shadow: 0 5px 15px rgba(167,201,87,0.6);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .btn-play:hover {
      background: linear-gradient(45deg, #a7c957, #6a994e);
      box-shadow: 0 8px 25px rgba(106,153,78,0.9);
      transform: scale(1.1);
    }
  </style>
</head>
<body>

  <div class="container-opening">
    <div class="logo" id="logo">Candy Crush</div>
    <div class="subtitle" id="subtitle">Match the sweetest candies!</div>
    <button class="btn btn-play" id="btnPlay">Jogar</button>
  </div>

  <script>
    // Animação do logo - efeito de entrada letter by letter
    const logo = document.getElementById('logo');
    logo.innerHTML = logo.textContent.replace(/\S/g, "<span class='letter'>$&</span>");

    anime.timeline({loop: false})
      .add({
        targets: '#logo .letter',
        translateY: [50, 0],
        opacity: [0,1],
        easing: "easeOutExpo",
        duration: 800,
        delay: (el, i) => 30 * i
      })
      .add({
        targets: '#subtitle',
        opacity: [0, 1],
        translateY: [20, 0],
        easing: 'easeOutExpo',
        duration: 1000,
        delay: 200
      })
      .add({
        targets: '#btnPlay',
        opacity: [0, 1],
        scale: [0.7, 1],
        easing: 'easeOutElastic(1, .6)',
        duration: 1500
      });

    // Evento clique no botão jogar
    document.getElementById('btnPlay').addEventListener('click', () => {
      alert('Vamos começar a jogar! 🎉');
      // Aqui pode iniciar a transição para a próxima tela do jogo
    });
  </script>

</body>
</html>
